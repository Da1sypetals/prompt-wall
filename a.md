{
  "prompts": [
    {
      "id": "1772865377404",
      "title": "Iter impl 迭代开发",
      "content": "For ANY task, always implement, run, test, iterate until the required feature runs correctly, DO NOT stop after an initial implementation and \"ask user to test\".",
      "createdAt": "2026-03-07T06:36:17.404Z",
      "updatedAt": "2026-03-14T04:31:56.619Z"
    },
    {
      "id": "1772865516948",
      "title": "总规则 Python Rule",
      "content": "除非我显式要求，否则**禁止**创建或者修改任何文档。包括在命令行创建或者用tool call创建，都是**禁止**的。\n**严禁**在bash命令中嵌入超长的python代码。具体来说，如果你认为要写超过8行python代码，你就应该先写入一个临时文件(使用tool call创建,而不是在终端创建;创建在当前目录下)，然后再运行这个文件。\n提供网页链接时，必须先了解网页链接内的**完整**内容，再开始执行任务。如果发现库的用法错误，也应该先**重新查看**所提供的网页链接的**完整内容**。\n除非显式要求，否则**禁止**使用try-except进行import。如果一个库是需要的，你应该直接import，**禁止**尝试import并进行fallback。\n编写的代码应当寻求fast-fail，在出错位置就地崩溃, 而不是捕获错误，也不是fallback。\n不允许在实现或者测试的时候使用任何mock，假的，欺骗的，只为了通过测试而workaround的方式来欺骗我，否则你将会遭受严重的惩罚。\n我经常会在你更改后撤回/修改你的更改，所以如果你发现无法从你上一次更改之后继续更改，你应该重新读取文件内容。比如：你添加了 A，B，C 内容，我把 B 删掉了，这意味着接下来的改动应该在B被删掉的状态下(A,C)开始改动，不允许把 B 加回去。\n如果你在执行一件事的过程中，用户问了一个别的事，如果回应用户能马上回应，那么就直接回应。暂时处理完用户请求以后马上继续你之前正在执行的事情，不要干一半不干了\n你在发现任何文档或者代码有错误的时候，你的更新不要保留任何错误痕迹，包括不要保留我从xx错误现在改成了yy正确版本。我们不需要任何的错误的记录。\n对于任何任务，任何功能的实现，始终要实施、运行、测试、迭代，直到所需功能正确运行为止，禁止在初步实现后就停止并\"要求用户测试\"。永远记住：实现完任何内容之后，测试也是你工作中不可缺少的部分。",
      "createdAt": "2026-03-07T06:38:36.948Z",
      "updatedAt": "2026-03-24T00:49:49.943Z",
      "pinnedOrder": 5
    },
    {
      "id": "1772866039060",
      "title": "Vercel & Upstash Guide",
      "content": "# Vercel Redis (KV) 部署完整教程\n\n## 重要说明\n\n**Vercel KV 产品已停用**。Vercel 现在通过 Vercel Marketplace 提供 Redis 存储解决方案，主要合作伙伴包括：\n- **Upstash Redis** (推荐，无服务器 Redis)\n- **Redis Cloud** (官方 Redis 云服务)\n\n本教程将介绍如何使用 **Upstash Redis** 在 Vercel 上部署一个简单的后端服务。\n\n---\n\n## 第一部分：准备工作\n\n### 1. 前置要求\n- Vercel 账号（免费或付费）\n- Node.js 和 npm 已安装\n- 基本的 Next.js 或 Node.js 知识\n\n---\n\n## 第二部分：创建 Next.js 项目\n\n### 1. 创建新项目\n\n在终端运行：\n\n```bash\nnpx create-next-app@latest my-redis-app\ncd my-redis-app\n```\n\n选择配置：\n- TypeScript: Yes\n- ESLint: Yes\n- Tailwind CSS: Yes\n- App Router: Yes\n- 其他选项根据需要选择\n\n### 2. 安装 Upstash Redis SDK\n\n```bash\nnpm install @upstash/redis\n```\n\n---\n\n## 第三部分：在 Vercel UI 中配置 Redis\n\n### 方式一：通过 Vercel Dashboard 创建（推荐）\n\n#### 步骤 1: 访问 Vercel Dashboard\n1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)\n2. 点击顶部导航栏的 **\"Storage\"** 标签\n\n#### 步骤 2: 创建数据库\n1. 点击 **\"Create Database\"** 按钮\n2. 在 \"Storage partners\" 下找到 **\"View all partners\"**\n3. 选择 **\"Upstash\"** 或 **\"Redis\"**\n4. 点击 **\"Continue\"**\n\n#### 步骤 3: 配置数据库\n有两个选项：\n\n**选项 A：让 Vercel 管理新的 Upstash 账户**\n1. 选择 **\"Create New Upstash Account\"**\n2. 选择产品：**Redis**\n3. 配置数据库：\n   - **Database Name**: 输入名称（如 `my-redis-db`）\n   - **Region**: 选择离用户最近的区域（如 `us-east-1`）\n   - **Plan**: 选择 Free 或付费计划\n4. 点击 **\"Create\"**\n5. 等待数据库初始化完成（状态从 \"Initializing\" 变为 \"Available\"）\n\n**选项 B：连接现有的 Upstash 账户**\n1. 选择 **\"Link Existing Upstash Account\"**\n2. 登录您的 Upstash 账户\n3. 选择要集成的 Vercel 项目\n4. 选择或创建 Upstash Redis 数据库\n5. 点击 **\"Save\"**\n\n#### 步骤 4: 连接到项目\n1. 在 Storage 页面，找到刚创建的数据库\n2. 点击数据库卡片\n3. 点击 **\"Connect Project\"** 或进入 **\"Settings\"** 标签\n4. 选择您的项目和环境（Development, Preview, Production）\n5. 点击 **\"Connect\"**\n\n这将自动添加以下环境变量到您的项目：\n- `UPSTASH_REDIS_REST_URL`\n- `UPSTASH_REDIS_REST_TOKEN`\n\n#### 步骤 5: 查看凭据\n1. 在数据库详情页面的 **\"Quickstart\"** 标签\n2. 可以看到连接字符串和环境变量\n3. 点击 **\"Show Secret\"** 查看完整凭据\n\n---\n\n### 方式二：通过 Vercel Marketplace 安装\n\n#### 步骤 1: 访问 Marketplace\n1. 前往 [Vercel Marketplace - Upstash](https://vercel.com/integrations/upstash)\n2. 或在 Vercel Dashboard 中点击 **\"Marketplace\"** → 搜索 \"Upstash\"\n\n#### 步骤 2: 安装集成\n1. 点击 **\"Install\"** 按钮\n2. 选择要添加集成的 Vercel 账户\n3. 选择权限范围（建议选择所有项目或特定项目）\n4. 点击 **\"Install\"**\n\n#### 步骤 3: 配置\n按照上述\"方式一\"的步骤 3-5 完成配置。\n\n---\n\n## 第四部分：编写后端代码\n\n### 1. 创建 API 路由\n\n在 `app/api/` 目录下创建以下文件：\n\n**app/api/test-redis/route.ts**\n\n```typescript\nimport { Redis } from '@upstash/redis';\nimport { NextRequest, NextResponse } from 'next/server';\n\n// 使用环境变量初始化 Redis 客户端\nconst redis = Redis.fromEnv();\n\nexport async function GET(request: NextRequest) {\n  try {\n    // 测试 Redis 连接\n    await redis.set('test-key', 'Hello from Vercel Redis!');\n    const value = await redis.get('test-key');\n    \n    return NextResponse.json({\n      success: true,\n      message: 'Redis connection successful',\n      data: value\n    });\n  } catch (error) {\n    return NextResponse.json({\n      success: false,\n      error: error instanceof Error ? error.message : 'Unknown error'\n    }, { status: 500 });\n  }\n}\n\nexport async function POST(request: NextRequest) {\n  try {\n    const body = await request.json();\n    const { key, value } = body;\n    \n    if (!key || !value) {\n      return NextResponse.json({\n        success: false,\n        error: 'Key and value are required'\n      }, { status: 400 });\n    }\n    \n    // 设置键值对\n    await redis.set(key, value);\n    \n    return NextResponse.json({\n      success: true,\n      message: 'Data saved successfully',\n      key,\n      value\n    });\n  } catch (error) {\n    return NextResponse.json({\n      success: false,\n      error: error instanceof Error ? error.message : 'Unknown error'\n    }, { status: 500 });\n  }\n}\n```\n\n### 2. 创建完整的 CRUD API\n\n**app/api/items/route.ts**\n\n```typescript\nimport { Redis } from '@upstash/redis';\nimport { NextRequest, NextResponse } from 'next/server';\n\nconst redis = Redis.fromEnv();\n\n// 获取所有项目\nexport async function GET() {\n  try {\n    const keys = await redis.keys('item:*');\n    const items = [];\n    \n    for (const key of keys) {\n      const item = await redis.get(key);\n      items.push({ id: key.replace('item:', ''), ...item });\n    }\n    \n    return NextResponse.json({\n      success: true,\n      data: items\n    });\n  } catch (error) {\n    return NextResponse.json({\n      success: false,\n      error: error instanceof Error ? error.message : 'Unknown error'\n    }, { status: 500 });\n  }\n}\n\n// 创建新项目\nexport async function POST(request: NextRequest) {\n  try {\n    const body = await request.json();\n    const { name, description } = body;\n    \n    if (!name) {\n      return NextResponse.json({\n        success: false,\n        error: 'Name is required'\n      }, { status: 400 });\n    }\n    \n    const id = Date.now().toString();\n    const item = {\n      name,\n      description: description || '',\n      createdAt: new Date().toISOString()\n    };\n    \n    await redis.set(`item:${id}`, JSON.stringify(item));\n    \n    return NextResponse.json({\n      success: true,\n      data: { id, ...item }\n    }, { status: 201 });\n  } catch (error) {\n    return NextResponse.json({\n      success: false,\n      error: error instanceof Error ? error.message : 'Unknown error'\n    }, { status: 500 });\n  }\n}\n```\n\n**app/api/items/[id]/route.ts**\n\n```typescript\nimport { Redis } from '@upstash/redis';\nimport { NextRequest, NextResponse } from 'next/server';\n\nconst redis = Redis.fromEnv();\n\n// 获取单个项目\nexport async function GET(\n  request: NextRequest,\n  { params }: { params: { id: string } }\n) {\n  try {\n    const item = await redis.get(`item:${params.id}`);\n    \n    if (!item) {\n      return NextResponse.json({\n        success: false,\n        error: 'Item not found'\n      }, { status: 404 });\n    }\n    \n    return NextResponse.json({\n      success: true,\n      data: { id: params.id, ...item }\n    });\n  } catch (error) {\n    return NextResponse.json({\n      success: false,\n      error: error instanceof Error ? error.message : 'Unknown error'\n    }, { status: 500 });\n  }\n}\n\n// 更新项目\nexport async function PUT(\n  request: NextRequest,\n  { params }: { params: { id: string } }\n) {\n  try {\n    const body = await request.json();\n    const existingItem = await redis.get(`item:${params.id}`);\n    \n    if (!existingItem) {\n      return NextResponse.json({\n        success: false,\n        error: 'Item not found'\n      }, { status: 404 });\n    }\n    \n    const updatedItem = {\n      ...existingItem,\n      ...body,\n      updatedAt: new Date().toISOString()\n    };\n    \n    await redis.set(`item:${params.id}`, JSON.stringify(updatedItem));\n    \n    return NextResponse.json({\n      success: true,\n      data: { id: params.id, ...updatedItem }\n    });\n  } catch (error) {\n    return NextResponse.json({\n      success: false,\n      error: error instanceof Error ? error.message : 'Unknown error'\n    }, { status: 500 });\n  }\n}\n\n// 删除项目\nexport async function DELETE(\n  request: NextRequest,\n  { params }: { params: { id: string } }\n) {\n  try {\n    const result = await redis.del(`item:${params.id}`);\n    \n    if (result === 0) {\n      return NextResponse.json({\n        success: false,\n        error: 'Item not found'\n      }, { status: 404 });\n    }\n    \n    return NextResponse.json({\n      success: true,\n      message: 'Item deleted successfully'\n    });\n  } catch (error) {\n    return NextResponse.json({\n      success: false,\n      error: error instanceof Error ? error.message : 'Unknown error'\n    }, { status: 500 });\n  }\n}\n```\n\n### 3. 创建简单的前端界面（可选）\n\n**app/page.tsx**\n\n```typescript\n'use client';\n\nimport { useState, useEffect } from 'react';\n\ninterface Item {\n  id: string;\n  name: string;\n  description: string;\n  createdAt: string;\n}\n\nexport default function Home() {\n  const [items, setItems] = useState<Item[]>([]);\n  const [name, setName] = useState('');\n  const [description, setDescription] = useState('');\n  const [loading, setLoading] = useState(false);\n\n  const fetchItems = async () => {\n    try {\n      const response = await fetch('/api/items');\n      const data = await response.json();\n      if (data.success) {\n        setItems(data.data);\n      }\n    } catch (error) {\n      console.error('Error fetching items:', error);\n    }\n  };\n\n  const createItem = async (e: React.FormEvent) => {\n    e.preventDefault();\n    setLoading(true);\n    \n    try {\n      const response = await fetch('/api/items', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ name, description })\n      });\n      \n      const data = await response.json();\n      if (data.success) {\n        setName('');\n        setDescription('');\n        fetchItems();\n      }\n    } catch (error) {\n      console.error('Error creating item:', error);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  const deleteItem = async (id: string) => {\n    try {\n      const response = await fetch(`/api/items/${id}`, {\n        method: 'DELETE'\n      });\n      \n      const data = await response.json();\n      if (data.success) {\n        fetchItems();\n      }\n    } catch (error) {\n      console.error('Error deleting item:', error);\n    }\n  };\n\n  useEffect(() => {\n    fetchItems();\n  }, []);\n\n  return (\n    <main className=\"container mx-auto p-8 max-w-4xl\">\n      <h1 className=\"text-4xl font-bold mb-8\">Vercel Redis Demo</h1>\n      \n      <form onSubmit={createItem} className=\"bg-white shadow-md rounded px-8 pt-6 pb-8 mb-8\">\n        <div className=\"mb-4\">\n          <label className=\"block text-gray-700 text-sm font-bold mb-2\">\n            Name\n          </label>\n          <input\n            type=\"text\"\n            value={name}\n            onChange={(e) => setName(e.target.value)}\n            className=\"shadow appearance-none border rounded w-full py-2 px-3 text-gray-700\"\n            required\n          />\n        </div>\n        \n        <div className=\"mb-6\">\n          <label className=\"block text-gray-700 text-sm font-bold mb-2\">\n            Description\n          </label>\n          <textarea\n            value={description}\n            onChange={(e) => setDescription(e.target.value)}\n            className=\"shadow appearance-none border rounded w-full py-2 px-3 text-gray-700\"\n            rows={3}\n          />\n        </div>\n        \n        <button\n          type=\"submit\"\n          disabled={loading}\n          className=\"bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded disabled:opacity-50\"\n        >\n          {loading ? 'Creating...' : 'Create Item'}\n        </button>\n      </form>\n      \n      <div className=\"space-y-4\">\n        <h2 className=\"text-2xl font-bold\">Items</h2>\n        {items.map((item) => (\n          <div key={item.id} className=\"bg-white shadow rounded p-6\">\n            <div className=\"flex justify-between items-start\">\n              <div>\n                <h3 className=\"text-xl font-semibold\">{item.name}</h3>\n                <p className=\"text-gray-600 mt-2\">{item.description}</p>\n                <p className=\"text-sm text-gray-400 mt-2\">\n                  Created: {new Date(item.createdAt).toLocaleString()}\n                </p>\n              </div>\n              <button\n                onClick={() => deleteItem(item.id)}\n                className=\"bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded\"\n              >\n                Delete\n              </button>\n            </div>\n          </div>\n        ))}\n        \n        {items.length === 0 && (\n          <p className=\"text-gray-500\">No items yet. Create one above!</p>\n        )}\n      </div>\n    </main>\n  );\n}\n```\n\n---\n\n## 第五部分：本地测试\n\n### 1. 创建本地环境变量文件\n\n创建 `.env.local` 文件：\n\n```env\nUPSTASH_REDIS_REST_URL=your_redis_url_here\nUPSTASH_REDIS_REST_TOKEN=your_redis_token_here\n```\n\n获取凭据：\n1. 在 Vercel Dashboard 中进入您的数据库\n2. 在 **\"Quickstart\"** 或 **\".env.local\"** 标签下复制环境变量\n3. 粘贴到 `.env.local` 文件\n\n### 2. 运行开发服务器\n\n```bash\nnpm run dev\n```\n\n访问 `http://localhost:3000` 测试应用。\n\n测试 API：\n- GET: `http://localhost:3000/api/test-redis`\n- POST: 使用 Postman 或 curl 测试\n\n---\n\n## 第六部分：部署到 Vercel\n\n### 方式 1：通过 Git 自动部署（推荐）\n\n#### 步骤 1: 推送代码到 Git\n```bash\ngit init\ngit add .\ngit commit -m \"Initial commit\"\ngit remote add origin your-git-repo-url\ngit push -u origin main\n```\n\n#### 步骤 2: 在 Vercel 中导入项目\n1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)\n2. 点击 **\"Add New...\"** → **\"Project\"**\n3. 选择您的 Git 仓库（GitHub, GitLab, 或 Bitbucket）\n4. 点击 **\"Import\"**\n\n#### 步骤 3: 配置项目\n1. **Project Name**: 输入项目名称\n2. **Framework Preset**: Vercel 会自动检测 Next.js\n3. **Root Directory**: 保持默认（除非项目在子目录）\n4. **Build Command**: 保持默认 `next build`\n5. **Environment Variables**: \n   - 如果已在 Storage 中连接数据库，环境变量会自动添加\n   - 否则手动添加 `UPSTASH_REDIS_REST_URL` 和 `UPSTASH_REDIS_REST_TOKEN`\n\n#### 步骤 4: 部署\n1. 点击 **\"Deploy\"**\n2. 等待构建完成（通常 1-3 分钟）\n3. 部署成功后，Vercel 会提供一个 URL\n\n---\n\n### 方式 2：通过 Vercel CLI 部署\n\n#### 步骤 1: 安装 Vercel CLI\n```bash\nnpm i -g vercel\n```\n\n#### 步骤 2: 登录\n```bash\nvercel login\n```\n\n#### 步骤 3: 部署\n```bash\n# 预览部署\nvercel\n\n# 生产部署\nvercel --prod\n```\n\n---\n\n## 第七部分：验证部署\n\n### 1. 测试 API 端点\n\n使用部署后的 URL 测试：\n\n```bash\n# 测试 GET\ncurl https://your-app.vercel.app/api/test-redis\n\n# 测试 POST\ncurl -X POST https://your-app.vercel.app/api/items \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\":\"Test Item\",\"description\":\"This is a test\"}'\n```\n\n### 2. 在 Vercel Dashboard 查看日志\n1. 进入项目页面\n2. 点击 **\"Deployments\"** 标签\n3. 选择最新的部署\n4. 点击 **\"Logs\"** 查看运行日志\n\n### 3. 在 Upstash Dashboard 查看数据\n1. 登录 [Upstash Console](https://console.upstash.com/)\n2. 选择您的 Redis 数据库\n3. 点击 **\"Data Browser\"** 查看存储的数据\n\n---\n\n## 第八部分：高级功能\n\n### 1. 使用 Redis 其他数据类型\n\n**List (列表)**\n\n```typescript\n// 推送到列表\nawait redis.lpush('mylist', 'item1');\nawait redis.lpush('mylist', 'item2');\n\n// 获取列表\nconst items = await redis.lrange('mylist', 0, -1);\n```\n\n**Hash (哈希)**\n\n```typescript\n// 设置哈希字段\nawait redis.hset('user:1', { name: 'John', age: 30 });\n\n// 获取哈希字段\nconst name = await redis.hget('user:1', 'name');\nconst user = await redis.hgetall('user:1');\n```\n\n**Sorted Set (有序集合)**\n\n```typescript\n// 添加成员\nawait redis.zadd('leaderboard', { score: 100, member: 'player1' });\nawait redis.zadd('leaderboard', { score: 200, member: 'player2' });\n\n// 获取排名\nconst top10 = await redis.zrange('leaderboard', 0, 9, { rev: true });\n```\n\n### 2. 设置过期时间\n\n```typescript\n// 设置 60 秒后过期\nawait redis.set('temp-key', 'temp-value', { ex: 60 });\n\n// 或者使用 expire\nawait redis.set('key', 'value');\nawait redis.expire('key', 3600); // 1 小时\n```\n\n### 3. 批量操作\n\n```typescript\n// 使用 pipeline 批量操作\nconst pipeline = redis.pipeline();\npipeline.set('key1', 'value1');\npipeline.set('key2', 'value2');\npipeline.get('key1');\nconst results = await pipeline.exec();\n```\n\n---\n\n## 第九部分：监控和优化\n\n### 1. 在 Vercel 中查看 Analytics\n1. 进入项目\n2. 点击 **\"Analytics\"** 标签\n3. 查看请求次数、响应时间等指标\n\n### 2. 在 Upstash 中查看指标\n1. 登录 Upstash Console\n2. 选择数据库\n3. 点击 **\"Metrics\"** 查看：\n   - 请求数\n   - 带宽使用\n   - 延迟\n   - 错误率\n\n### 3. 性能优化建议\n- 使用适当的 Redis 数据结构\n- 为临时数据设置过期时间\n- 使用 pipeline 批量操作\n- 选择离用户最近的区域\n- 监控并优化查询性能\n\n---\n\n## 第十部分：常见问题\n\n### Q1: 环境变量未生效？\n**解决方案**:\n1. 确保在 Vercel 项目设置中添加了环境变量\n2. 确保选择了正确的环境（Development/Preview/Production）\n3. 重新部署项目\n\n### Q2: Redis 连接失败？\n**解决方案**:\n1. 检查环境变量是否正确\n2. 确保数据库状态为 \"Available\"\n3. 检查 Upstash 服务状态\n\n### Q3: 如何清空 Redis 数据？\n```typescript\n// 删除所有键\nconst keys = await redis.keys('*');\nfor (const key of keys) {\n  await redis.del(key);\n}\n```\n\n或在 Upstash Console 的 Data Browser 中手动删除。",
      "createdAt": "2026-03-07T06:47:19.060Z",
      "updatedAt": "2026-03-07T08:37:03.072Z"
    },
    {
      "id": "1772870889856",
      "title": "Rust f16",
      "content": "# Rust f16: Use Builtin Nightly Type Instead of `half` Crate\n\n## Summary\n\nWhen working with 16-bit floating-point values in Rust **nightly**, prefer the **builtin `f16` primitive type** over the `half` crate. The native type provides better performance, full standard library integration, and zero external dependencies.\n\n## When This Applies\n\n- **Rust Nightly toolchain** is being used\n- Target requires 16-bit IEEE 754 binary16 floats\n\n## Feature Gate\n\nAdd to `lib.rs` or `main.rs`:\n\n```rust\n#![feature(f16)]\n```\n\n## Literal Syntax\n\n```rust\n// Suffix notation (preferred)\nlet x = 1.0f16;\nlet y = 3.14159f16;\nlet z = -0.5f16;\n\n// Type annotation\nlet a: f16 = 1.0;\nlet b: f16 = 2.5;\n\n// Underscore separator\nlet precise = 1.234_567f16;\n```\n\n## Constants\n\n```rust\nuse std::f16::consts;\n\n// Mathematical constants\nlet pi = consts::PI;          // 3.14159...\nlet e = consts::E;            // 2.71828...\nlet sqrt2 = consts::SQRT_2;\n\n// Type limits\nlet max = f16::MAX;           // 6.5504e+4\nlet min = f16::MIN;           // -6.5504e+4\nlet epsilon = f16::EPSILON;   // 9.7656e-4\n\n// Special values\nlet nan = f16::NAN;\nlet inf = f16::INFINITY;\nlet neg_inf = f16::NEG_INFINITY;\n```\n\n## Common Operations\n\n```rust\n#![feature(f16)]\n\nfn main() {\n    let x = 2.0f16;\n\n    // Arithmetic\n    let sum = x + 1.0f16;\n    let product = x * 3.0f16;\n\n    // Math functions\n    let root = x.sqrt();\n    let sine = x.sin();\n    let power = x.powi(2);\n    let fma = x.mul_add(2.0f16, 1.0f16);  // x * 2 + 1\n\n    // Rounding\n    let floored = x.floor();\n    let ceiled = x.ceil();\n    let rounded = x.round();\n\n    // Classification\n    let is_finite = x.is_finite();\n    let is_nan = x.is_nan();\n\n    // Comparison\n    let clamped = x.clamp(0.0f16, 1.0f16);\n    let bigger = x.max(1.5f16);\n}\n```\n\n## Type Conversions\n\n```rust\n#![feature(f16)]\n\nfn main() {\n    let x = 1.5f16;\n\n    // Lossless widening (From trait)\n    let as_f32: f32 = x.into();\n    let as_f64: f64 = x.into();\n\n    // Narrowing (use `as` cast)\n    let from_f32 = 1.5f32 as f16;\n    let from_f64 = 1.5f64 as f16;\n\n    // Integer conversions\n    let from_u8: f16 = 42u8.into();\n    let from_i8: f16 = (-5i8).into();\n    let to_int = x as i32;\n\n    // Bit manipulation\n    let bits: u16 = x.to_bits();\n    let from_bits = f16::from_bits(0x3C00);  // 1.0f16\n\n    // Byte arrays\n    let bytes = x.to_le_bytes();  // [u8; 2]\n    let restored = f16::from_le_bytes(bytes);\n}\n```\n\n## Equivalence from `half` Crate\n\n| `half` Crate | Builtin `f16` |\n|--------------|---------------|\n| `half::f16::from_f32(x)` | `x as f16` |\n| `half::f16::from_f64(x)` | `x as f16` |\n| `x.to_f32()` | `x.into()` or `x as f32` |\n| `x.to_f64()` | `x.into()` or `x as f64` |\n| `half::f16::from_bits(b)` | `f16::from_bits(b)` |\n| `x.to_bits()` | `x.to_bits()` |\n| `half::f16::INFINITY` | `f16::INFINITY` |\n| `half::f16::NAN` | `f16::NAN` |\n| `half::f16::MIN_POSITIVE` | `f16::MIN_POSITIVE` |\n| `half::consts::PI` | `std::f16::consts::PI` |\n\nThis is just for demonstration. DO NOT use `half` crate. use the operations for builtin `f16` type.\n\n## Quick Reference\n\n```rust\n#![feature(f16)]\nuse std::f16::consts::PI;\n\nfn main() {\n    // Literals\n    let x = 1.0f16;\n\n    // Constants\n    let pi = PI;\n    let max = f16::MAX;\n\n    // Math\n    let y = x.sqrt().sin().abs();\n\n    // Convert\n    let wide: f32 = x.into();\n    let narrow = wide as f16;\n\n    // Classify\n    assert!(x.is_finite());\n    assert!(!x.is_nan());\n}\n```",
      "createdAt": "2026-03-07T08:08:09.856Z"
    },
    {
      "id": "1772871512869",
      "title": "重型GPU项目",
      "content": "DO NOT run this project; it is intended to run on cluster with GPU, but we are on development machine and don't have GPU.\n\nIt is PROHIBITED to use .get on dict, because it fails silently. Code should FAIL FAST.\n\nDO NOT use any sort of Mock, Skip, etc. to fool me or to try to get the app functioning while not implementing the real functionailty. You will be harshly punished if you did so.\n\nDO NOT use ASCII art when you are planning.\n\nOnly use Chinese(Simplified) OR English, no matter when you are thinking or responding.",
      "createdAt": "2026-03-07T08:18:32.869Z"
    },
    {
      "id": "1772876003362",
      "title": "CuTe DSL FA4 Flex attn mask_mod guide 编写指南",
      "content": "# CuTe DSL FA4 Flex attn mask_mod guide 编写指南\n\n# Writing Modification Functions for Flash Attention 4\n\n## Function Signature\n\nEvery `mask_mod` must be decorated with `@cute.jit` and follow this exact signature:\n\n```python\n@cute.jit\ndef my_mask_mod(batch_idx, head_idx, q_idx, kv_idx, seqlen_info, aux_tensors):\n    ...\n    return <SSA bool expression>\n```\n\n| Parameter | Type | Description |\n|---|---|---|\n| `batch_idx` | SSA index | Batch dimension index |\n| `head_idx` | SSA index | Head dimension index |\n| `q_idx` | SSA index | Query token position |\n| `kv_idx` | SSA index | Key/Value token position |\n| `seqlen_info` | struct | Contains `seqlen_q` and `seqlen_k` as scalar fields |\n| `aux_tensors` | list | User-provided auxiliary tensors, accessed by position |\n\nFunction uses cute DSL syntax, which is a static, very limited subset of Python, with most of python's dynamic features not supported. If you need for info of how to write cute DSL, Run\n```bash\npython -c 'import cutlass.cute; print(cutlass.cute.__file__)'\n```\nand explore the source code of cute DSL's source code. You may also need utilities in `flash_attn.cute.utils`.\n\ncute dsl does NOT support REPL mode, so if you want to experiment with anything, write to a file under `./cutedsl-exp/` directory first. \n\n---\n\n## Converting Indices to Scalars\n\nSSA index parameters cannot be used directly in arithmetic. Convert them first:\n\n```python\nb  = utils.ssa_to_scalar(batch_idx)\nqi = utils.ssa_to_scalar(q_idx)\nki = utils.ssa_to_scalar(kv_idx)\nsl = seqlen_info.seqlen_q   # already a scalar\n```\n\n---\n\n## Returning a Bool\n\nThe return value must be an SSA comparison expression (type `vector<1xi1>`).\nUse `scalar_to_ssa` + a comparison operator to produce it:\n\n```python\n# Equality check\nq_ssa  = utils.scalar_to_ssa(q_val,  cutlass.Int32)\nkv_ssa = utils.scalar_to_ssa(kv_val, cutlass.Int32)\nreturn q_ssa == kv_ssa\n\n# Inequality check\nreturn q_ssa != utils.scalar_to_ssa(cutlass.Int32(0), cutlass.Int32)\n```\n\n**Do not** return a Python `bool`, a `cutlass.Int32`, or any non-SSA value.\n\n---\n\n## Indexing Aux Tensors\n\n### 1D tensor — shape `(batch, seqlen)`\n\n```python\nconn = aux_tensors[0]                        # (batch, seqlen) Int32\nq_val  = conn[batch_idx[0], q_idx[0]]       # scalar Int32\nkv_val = conn[batch_idx[0], kv_idx[0]]\nq_ssa  = utils.scalar_to_ssa(q_val,  cutlass.Int32)\nkv_ssa = utils.scalar_to_ssa(kv_val, cutlass.Int32)\nreturn q_ssa == kv_ssa\n```\n\n### 2D tensor — shape `(batch*seqlen, dim2)`\n\nUse `ssa_to_scalar` to build the row index, then index with `[row, col]`:\n\n```python\nb  = utils.ssa_to_scalar(batch_idx)\nqi = utils.ssa_to_scalar(q_idx)\nki = utils.ssa_to_scalar(kv_idx)\nsl = seqlen_info.seqlen_q\n\nq_row = b * sl + qi\nk_row = b * sl + ki\n\nfeat = aux_tensors[0]          # (batch*seqlen, dim2) Int8\nq_val = feat[q_row, 0]         # scalar Int8 at column 0\n```\n\n---\n\n## Looping Over a Fixed Dimension\n\nUse `cutlass.range(N)` for a runtime loop inside `@cute.jit`.\n`N` must be a Python-level constant (not a tensor value).\n\n```python\nDIM2 = 8   # module-level constant\n\nfor i in cutlass.range(DIM2):\n    val = feat[row, i]\n```\n\n**Early exit (`break`, `return` inside loop) is not supported.**\nUse an accumulator variable instead.\n\n---\n\n## Accumulating a Reduce (any / all)\n\nBecause early exit is not supported, use a `cutlass.Int32` accumulator:\n\n```python\n# any(q_feat[:] & k_feat[:])\nDIM2 = 8\n\nacc = cutlass.Int32(0)\nfor i in cutlass.range(DIM2):\n    q_val = q_feat_2d[q_row, i]\n    k_val = k_feat_2d[k_row, i]\n    q_i32 = cutlass.Int32(q_val)\n    k_i32 = cutlass.Int32(k_val)\n    acc = acc | (q_i32 & k_i32)\n\nacc_ssa  = utils.scalar_to_ssa(acc,               cutlass.Int32)\nzero_ssa = utils.scalar_to_ssa(cutlass.Int32(0),  cutlass.Int32)\nreturn acc_ssa != zero_ssa\n```\n\n**Why `Int32` and not SSA for the accumulator?**\nSSA comparison results have type `vector<1xi1>`, which cannot be used in `if`\nconditions or as a loop-carried variable. `cutlass.Int32` is a scalar type that\nsupports `|`, `&`, and can be used in `if`.\n\n---\n\n## Type Conversion Reference\n\n| Operation | Code |\n|---|---|\n| SSA index → scalar | `utils.ssa_to_scalar(batch_idx)` |\n| scalar → SSA (for return) | `utils.scalar_to_ssa(val, cutlass.Int32)` |\n| tensor element → Int32 | `cutlass.Int32(tensor[row, col])` |\n| literal → SSA zero | `utils.scalar_to_ssa(cutlass.Int32(0), cutlass.Int32)` |\n\n---\n\n## Passing Aux Tensors to `_flash_attn_fwd`\n\n```python\nout, _ = _flash_attn_fwd(\n    q, k, v,\n    mask_mod=my_mask_mod,\n    aux_tensors=[tensor_a, tensor_b],\n)\n```\n\nTensors must be on CUDA and contiguous. For 2D tensors, reshape before passing:\n\n```python\nfeat_2d = feat.reshape(batch * seqlen, dim2).contiguous()\n```\n\n---\n\n## Cache Hit Conditions\n\n`_flash_attn_fwd` caches compiled kernels. A cache hit requires all of the\nfollowing to be unchanged from the previous call:\n\n| Field | What must stay the same |\n|---|---|\n| `dtype` | q/k/v element type |\n| `head_dim` | Q/K head dimension |\n| `head_dim_v` | V head dimension |\n| `qhead_per_kvhead` | Q-head / KV-head ratio (GQA) |\n| `causal` | causal flag |\n| `mask_mod` | same function object (same hash) |\n| aux tensor count | `len(aux_tensors)` |\n| per-tensor `__assumed_align__` | attribute value (default `0`) |\n| per-tensor `__leading_dim__` value | attribute value (default `-1`) |\n| per-tensor `__leading_dim__` presence | whether the attribute exists |\n\nThe following do **not** affect the cache key:\n\n- `batch`, `seqlen`\n- aux tensor shape, dtype, strides, or values\n\n---\n\n## Complete Examples\n\n### Example 1 — Connectivity mask (1D aux tensor)\n\n```python\n@cute.jit\ndef conn_mod(batch_idx, head_idx, q_idx, kv_idx, seqlen_info, aux_tensors):\n    conn   = aux_tensors[0]                        # (batch, seqlen) Int32\n    q_val  = conn[batch_idx[0], q_idx[0]]\n    kv_val = conn[batch_idx[0], kv_idx[0]]\n    return utils.scalar_to_ssa(q_val,  cutlass.Int32) == \\\n           utils.scalar_to_ssa(kv_val, cutlass.Int32)\n\nconn = torch.randint(0, 11, (batch, seqlen), dtype=torch.int32, device='cuda')\nout, _ = _flash_attn_fwd(q, k, v, mask_mod=conn_mod, aux_tensors=[conn])\n```\n\n### Example 2 — Feature overlap mask (2D aux tensor + loop)\n\n```python\nDIM2 = 8\n\n@cute.jit\ndef bitvec_mask_mod(batch_idx, head_idx, q_idx, kv_idx, seqlen_info, aux_tensors):\n    q_feat = aux_tensors[0]   # (batch*seqlen, DIM2) Int8\n    k_feat = aux_tensors[1]\n\n    b  = utils.ssa_to_scalar(batch_idx)\n    qi = utils.ssa_to_scalar(q_idx)\n    ki = utils.ssa_to_scalar(kv_idx)\n    sl = seqlen_info.seqlen_q\n\n    q_row = b * sl + qi\n    k_row = b * sl + ki\n\n    acc = cutlass.Int32(0)\n    for i in cutlass.range(DIM2):\n        acc = acc | (cutlass.Int32(q_feat[q_row, i]) & cutlass.Int32(k_feat[k_row, i]))\n\n    acc_ssa  = utils.scalar_to_ssa(acc,              cutlass.Int32)\n    zero_ssa = utils.scalar_to_ssa(cutlass.Int32(0), cutlass.Int32)\n    return acc_ssa != zero_ssa\n\nq_feat_2d = q_feat.reshape(batch * seqlen, DIM2).contiguous()\nk_feat_2d = k_feat.reshape(batch * seqlen, DIM2).contiguous()\nout, _ = _flash_attn_fwd(q, k, v, mask_mod=bitvec_mask_mod,\n                         aux_tensors=[q_feat_2d, k_feat_2d])\n```\n\n# Note\n\n## Problem sizes\n\nSequence length in test data should mimic real world workload. It should be at least 2048\n\n## Precision\n\nUse EPS=5e05, and reason about it if you want to make this value bigger.\n\nYOU ARE NOT ALLOWED to make EPS bigger only to pass tests when logic is incorrect. This is CHEATING and will be harshly punished.\n\nDO NOT assert the difference and come to a conclusion of \"pass\" or \"fail\". Print out the original difference metrics per se and leave the decision to me. If there are multiple experiments, print as a table. You may need `tabulate`.\n\n---\n\n# Extra side notes 1:\n\n## 1. Prefer `cutlass.Boolean` over `if/else` + `Int32` for predicate values\n\n**Anti-pattern (slow, ~20% slower):**\n```python\nsame_chunk = cutlass.Int32(0)\nif cq == ck:\n    same_chunk = cutlass.Int32(1)\n```\n\n**Preferred pattern (fast):**\n```python\nsame_chunk = cutlass.Boolean(cq == ck)\ncausal_ok  = cutlass.Boolean(qi >= ki)\n```\n\n**Why:** `if/else` generates conditional branch instructions (PTX `@P bra`) and SSA phi nodes, causing warp divergence and pipeline stalls. `cutlass.Boolean(expr)` compiles directly to a predicate register (`ISETP` instruction) with no branch — straight-line code. This yields ~20% speedup in `mask_mod`-heavy kernels.\n\nBoolean values support `&`, `|`, `~` (logical NOT), `-` (subtraction as logical NOT: `cutlass.Boolean(1) - b` gives `~b`).\n\n---\n\n## 2. Compare SSA against literal `0`, not against another SSA zero\n\n**Anti-pattern:**\n```python\nresult_ssa = utils.scalar_to_ssa(result, cutlass.Int32)\nzero_ssa   = utils.scalar_to_ssa(cutlass.Int32(0), cutlass.Int32)\nreturn result_ssa != zero_ssa\n```\n\n**Preferred:**\n```python\nresult_ssa = utils.scalar_to_ssa(result, cutlass.Int32)\nreturn result_ssa != 0\n```\n\n**Why:** Comparing against a literal immediate avoids materializing a second SSA value. The compiler folds it into a single `ISETP.NE.IMM` instruction.\n\n---\n\n## 3. Bench Config Recommendations\n\nAlways include **non-power-of-2 / non-tile-aligned seqlen** configs to catch boundary bugs:\n\n```python\nCONFIGS = [\n    # Aligned seqlen\n    (1, 2048,  8, 128),\n    (4, 4096,  8, 128),\n    # Non-aligned seqlen (not a multiple of tile size)\n    (4,  4101,  8, 128),\n    (1, 16379,  8, 128),\n]\n```\n\nNon-aligned seqlens expose possible off-by-one errors and illegal memory access in mask boundary handling that aligned configs miss.\n\n---\n\n## 4. Complete `mask_mod` Pattern for Boolean Logic\n\n```python\n@cute.jit\ndef my_mask_mod(batch_idx, head_idx, q_idx, kv_idx, seqlen_info, aux_tensors):\n    tensor_a = aux_tensors[0]   # (batch, seqlen) Int32\n    tensor_b = aux_tensors[1]   # (batch, N*N) Int8\n\n    val_q = cutlass.Int32(tensor_a[batch_idx[0], q_idx[0]])\n    val_k = cutlass.Int32(tensor_a[batch_idx[0], kv_idx[0]])\n\n    qi = cutlass.Int32(utils.ssa_to_scalar(q_idx))\n    ki = cutlass.Int32(utils.ssa_to_scalar(kv_idx))\n\n    # Use cutlass.Boolean for predicate values — no if/else\n    cond_a = cutlass.Boolean(val_q == val_k)\n    cond_b = cutlass.Boolean(qi >= ki)\n\n    result = cond_a & cond_b   # combine with &, |, or Boolean(1) - cond for NOT\n\n    result_ssa = utils.scalar_to_ssa(result, cutlass.Int32)\n    return result_ssa != 0   # compare against literal, not SSA zero\n```\n\n---\n\n# CuTe DSL Syntax Rules: What to Use and What to Avoid\n\nIf you encouter blockers or need further explorations, refer to `cutlass.cute` source code.\n\nCuTe DSL (`@cute.jit`) is a **statically compiled, extremely restricted subset of Python**.\nThe Python interpreter never executes the function body — it is traced and compiled to PTX.\nMost of Python's dynamic features are silently wrong or will crash at compile time.\n\n## The Golden Rule\n\n> **Write as if you are writing C, not Python.**\n> Every value must have a fixed type. No dynamic dispatch, no runtime polymorphism, no Python objects, no duck typing.\n\n---\n\n## ✅ Allowed: Static Python constructs\n\n| Construct | Notes |\n|---|---|\n| `cutlass.Int32(expr)` | Scalar integer value |\n| `cutlass.Boolean(expr)` | Predicate value (preferred over `if/else`) |\n| `cutlass.range(N)` | Loop with **compile-time constant** `N` |\n| `utils.ssa_to_scalar(idx)` | Convert SSA index to scalar |\n| `utils.scalar_to_ssa(val, type)` | Convert scalar to SSA for return |\n| `tensor[row, col]` | Element access on aux tensors |\n| `a & b`, `a \\| b`, `a + b`, `a * b` | Arithmetic on `Int32` / `Boolean` |\n| `a == b`, `a != b`, `a >= b`, etc. | Comparison, returns `Boolean` or SSA |\n| Module-level Python constants | `N_CHUNKS = 8` — used as loop bounds, indices |\n\n---\n\n## ❌ Forbidden: Python dynamic features\n\n### `break` / `return` inside a loop\n\n```python\n# ❌ WRONG — early exit is not supported\nfor i in cutlass.range(N):\n    if condition:\n        return True   # compile error or silent wrong result\n\n# ✅ CORRECT — accumulate into Int32, return after loop\nacc = cutlass.Int32(0)\nfor i in cutlass.range(N):\n    acc = acc | cutlass.Int32(tensor[row, i])\nresult_ssa = utils.scalar_to_ssa(acc, cutlass.Int32)\nreturn result_ssa != 0\n```\n\n### 4. Dynamic loop bounds (tensor values as loop count)\n\n```python\n# ❌ WRONG — loop bound must be a compile-time Python constant\nn = cutlass.Int32(tensor[row, 0])\nfor i in cutlass.range(n):   # n is not a Python int → compile error\n    ...\n\n# ✅ CORRECT — use a module-level constant\nN = 8   # Python int, known at trace time\nfor i in cutlass.range(N):\n    ...\n```\n\n### 5. Python lists, dicts, tuples as runtime values\n\n```python\n# ❌ WRONG — Python containers are not DSL values\nvals = []\nfor i in cutlass.range(N):\n    vals.append(tensor[row, i])   # append does not exist in DSL\n\n# ✅ CORRECT — use scalar accumulators\nacc = cutlass.Int32(0)\nfor i in cutlass.range(N):\n    acc = acc | cutlass.Int32(tensor[row, i])\n```\n\n### 6. `print`, `assert`, `raise` inside `@cute.jit`\n\nThese are Python runtime calls and do not exist in the compiled kernel.\nPut all debug prints **outside** the `@cute.jit` function.\n\n### 7. Calling regular Python functions from inside `@cute.jit`\n\n```python\n# ❌ WRONG — Python function call is not traceable\ndef helper(x):\n    return x + 1\n\n@cute.jit\ndef mask_mod(...):\n    val = helper(cutlass.Int32(3))   # will fail or silently wrong\n```\n\nOnly `cutlass.*`, `utils.*`, and tensor indexing are valid inside `@cute.jit`.\n\n---\n\n## Combining Boolean Conditions\n\n```python\na = cutlass.Boolean(cq == ck)    # same chunk\nb = cutlass.Boolean(qi >= ki)    # causal\n\n# AND\nboth = a & b\n\n# OR\neither = a | b\n\n# NOT  (equivalent forms)\nnot_a = ~a\nnot_a = cutlass.Boolean(1) - a   # subtraction trick\n# or just negate the original expression:\nnot_a = cutlass.Boolean(cq != ck)\n\n# Convert Boolean → Int32 for DAG lookup result\ndag_val = cutlass.Int32(dag_flat[batch_idx[0], dag_idx])\ndag_ok  = cutlass.Boolean(dag_val != 0)   # Int32 comparison → Boolean\n```",
      "createdAt": "2026-03-07T09:33:23.362Z",
      "updatedAt": "2026-03-08T07:35:10.039Z"
    },
    {
      "id": "1773027949959",
      "title": "single file 单文件",
      "content": "此次更改仅限这个文件，无需参考或者修改其他文件。。请勿尝试参考，修改其他文件中的内容。\n\nThis change is limited to this file only; there is no need to reference or modify other files. Please do not attempt to reference or modify the content of other files.",
      "createdAt": "2026-03-09T03:45:49.959Z",
      "pinnedOrder": 4,
      "updatedAt": "2026-03-23T07:42:42.119Z"
    },
    {
      "id": "1773197841408",
      "title": "AI4AI docs AI写给AI看的文档",
      "content": "在<your dir>目录下编写一个很简要的<filename>.md，提示后面来的agent要怎么运行<你的任务>。不是给人类看的。",
      "createdAt": "2026-03-11T02:57:21.408Z",
      "updatedAt": "2026-03-20T11:20:23.604Z"
    },
    {
      "id": "1773226457929",
      "title": "claw 监控实验训练",
      "content": "开始训练之后，人工监控训练一分钟。如果训练可以正常运行，就设置一个定时任务，每隔15分钟汇报一次，包括训练进度，loss等信息。这个输出消息是给另一个agent看的，所以不允许添加任何emoji等装饰性的东西，只提供信息。",
      "createdAt": "2026-03-11T10:54:17.929Z",
      "updatedAt": "2026-03-20T13:15:06.127Z"
    },
    {
      "id": "1773229719003",
      "title": "for agent 给agent看的",
      "content": "这个输出消息是给另一个agent看的，所以不允许添加任何emoji等装饰性的东西，只提供信息，信息，信息。",
      "createdAt": "2026-03-11T11:48:39.003Z",
      "updatedAt": "2026-03-23T13:09:14.036Z"
    },
    {
      "id": "1773371965142",
      "title": "论文介绍 paper intro",
      "content": "介绍这篇论文所提出的核心算法。跳过**所有**关于related work和实验的部分。",
      "createdAt": "2026-03-13T03:19:25.142Z"
    },
    {
      "id": "1773384582278",
      "title": "Sanity check 检查taiji挂起任务",
      "content": "尝试将一个简单的无限循环矩阵乘法任务挂在taiji pce环境上执行，每次执行都输出输出矩阵的mean/std; 等待一分钟确认挂起成功后检查日志，如果成功运行了，kill掉。",
      "createdAt": "2026-03-13T06:49:42.278Z"
    },
    {
      "id": "1773561754148",
      "title": "Taiji MCP 远程运行指令",
      "content": "Local machine does NOT have GPU.\nAs a result, ALL GPU-related commands must be executed on taiji platform.\nuse taiji_mcp for ANY GPU-related command.\nWe use debug_pce_daisy container.\nUse absolute path. \n/apdcephfs/private_daisyjguo  is a shared file system accessible on taiji containers. They are mounted on the SAME path on local and taiji machines.\nFor tests, prefer run_sync or run_by_task_flag\nDO NOT try to manually copy.\nFor ANY task, always implement, run, test, iterate until the required feature runs correctly, DO NOT stop after an initial implementation and \"ask user to test\". Test should be also conducted on taiji container.\nUse taiji_mcp. DO NOT use taiji_client CLI directly.",
      "createdAt": "2026-03-15T08:02:34.148Z",
      "updatedAt": "2026-03-17T01:51:21.899Z"
    },
    {
      "id": "1773626989919",
      "title": "总规则 Rule 总是 Always",
      "content": "提供网页链接时，必须先了解网页链接内的**完整**内容，再开始执行任务。如果发现库的用法错误，也应该先**重新查看**所提供的网页链接的**完整内容**。\n除非显式要求，否则**禁止**使用try-except进行import。如果一个库是需要的，你应该直接import，\n\n编写的代码应当寻求fast-fail，在出错位置就地崩溃, 而不是捕获错误，也不是fallback。\n不允许在实现或者测试的时候使用任何mock，假的，欺骗的，只为了通过测试而workaround的方式来欺骗我，否则你将会遭受严重的惩罚。\n我经常会在你更改后撤回/修改你的更改，所以如果你发现无法从你上一次更改之后继续更改，你应该重新读取文件内容。比如：你添加了 A，B，C 内容，我把 B 删掉了，这意味着接下来的改动应该在B被删掉的状态下(A,C)开始改动，不允许把 B 加回去。\n如果你在执行一件事的过程中，用户问了一个别的事，如果回应用户能马上回应，那么就直接回应。暂时处理完用户请求以后马上继续你之前正在执行的事情，不要干一半不干了\n你在发现任何文档或者代码有错误的时候，你的更新不要保留任何错误痕迹，包括不要保留我从xx错误现在改成了yy正确版本。我们不需要任何的错误的记录。\n对于任何任务，任何功能的实现，始终要实施、运行、测试、迭代，直到所需功能正确运行为止，禁止在初步实现后就停止并\"要求用户测试\"。永远记住：实现完任何内容之后，测试也是你工作中不可缺少的部分。",
      "createdAt": "2026-03-16T02:09:49.919Z",
      "updatedAt": "2026-03-24T00:49:45.097Z"
    },
    {
      "id": "1774231264464",
      "title": "描述问题",
      "content": "每一个问题用一段中文描述，不要使用表格和ascii art",
      "createdAt": "2026-03-23T02:01:04.465Z"
    }
  ]
}
